<controls:Dialog x:Class="BSE.CoverFlow.WPFLib.PlaylistPanel.NewPlaylistDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:viewmodels="clr-namespace:BSE.CoverFlow.WPFLib.ViewModel"
                 xmlns:controls="clr-namespace:BSE.CoverFlow.WPFLib.Controls"
                 xmlns:p="clr-namespace:BSE.CoverFlow.WPFLib.Properties"
                 mc:Ignorable="d"
                 DialogOkCommand="{Binding DialogOkCommand}"
                 DialogCancelCommand="{Binding DialogCancelCommand}"
                 Title="{x:Static p:Resources.IDS_NewPlaylistDialog_Header}"
                 Height="200" Width="400">

    <Grid>
        <StackPanel VerticalAlignment="Top">
            <TextBox x:Uid="IDS_NewPlaylistDialog_Txt_PlaylistName" Text="{Binding Path=PlaylistName, UpdateSourceTrigger=PropertyChanged}" Height="23" Margin="0" HorizontalAlignment="Stretch" />
            <TextBlock x:Uid="IDS_NewPlaylistDialog_Tb_ErrorMessage" FontWeight="SemiBold" Text="{Binding Path=ErrorMessage}"/>
        </StackPanel>
    </Grid>
</controls:Dialog>
